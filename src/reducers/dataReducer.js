const data = (state = 0, action) => {
    switch (action.type) {
        case "increase":
            
            break;

        case "decrease":
            
            break;
    
        default:
            break;
    }
}

export default data;